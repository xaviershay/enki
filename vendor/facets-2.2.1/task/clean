#!/usr/bin/env ratch

# Remove pkg/ and doc/rdoc products

CLOBBER = %w{ pkg doc/rdoc }

main :clean do
  CLOBBER.each do |f|
    print path
    if exits?(f)
      rm_r(f)
      puts " (removed)"
    else
      puts " (missing)"
    end
  end
end
