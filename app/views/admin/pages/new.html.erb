<h1><%=  t('admin.pages.new.title') %></h1>

<%= semantic_form_for([:admin, @page]) do |form| -%>
  <%= render :partial => 'form', :locals => {:form => form} %>
  <%= form.actions do -%>
    <%= form.action :submit, :as => :button, t('admin.pages.form.save')-%>
  <% end -%>
<% end -%>
